function slove(one,two,three) {
    totalLenght = one.length + two.length + three.length
    averageLenght = Math.floor(totalLenght / 3)
    console.log(totalLenght)
    console.log(averageLenght)
}
slove('chocolate', 'ice cream', 'cake')